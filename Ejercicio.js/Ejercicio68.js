// Escribir un programa que muestre por consola la tabla de multiplicar del 1 al 10.
for(let i=1;1<=10;i++){
    alert(`La tabla de multiplciar ${i}`)
    for(let o=1;o<=10;o++){
        alert(`Es ${i} X ${o} es = ${i*o}`)
    }   
    if(i==10){
        break;
    }
}